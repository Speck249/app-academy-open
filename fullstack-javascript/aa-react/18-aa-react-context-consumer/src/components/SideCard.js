const SideCard = () => {
  return (
    <div className="side-card">
      {console.log("Renders or Re-renders")}
      <h1>React Context with Horoscopes</h1>
    </div>
  );
};

export default SideCard;
